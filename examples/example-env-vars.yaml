# Deploy ${APP_NAME} with environment-based paths

actions:
  - name: deploy-from-env
    action: deploy
    file: ${APP_BINARY}
    type: executable

  - name: print-target
    action: shell
    command: 'echo Prepared ${APP_NAME} from ${APP_BINARY}'
    fail_on_error: true
